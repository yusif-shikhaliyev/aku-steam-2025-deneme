// Firebase configuration is removed as per user request.
// This file is kept empty to prevent import errors in existing components until they are fully refactored.

export const db = null;
export const isFirebaseConfigured = false;
export const saveFirebaseConfig = (config: any) => {};
export const resetFirebaseConfig = () => {};